# Repository Instructions

- For every backend code change under `Backend/`, update the documentation in `Backend/docs/`.
- At minimum, update `Backend/docs/BACKEND_CHANGELOG.md` and any affected feature guide with the changed logic, routes, schemas, models, migrations, or configuration.
- If a backend change affects runtime behavior, document how to test it.

